CREATE TABLE IF NOT EXISTS GPetPromotionLog (PetUID INTEGER NOT NULL, LoginUID INTEGER NOT NULL, PetID INTEGER NOT NULL, OldPetName TEXT NOT NULL, NewPetName TEXT NOT NULL, OldPromotion INTEGER NOT NULL, NewPromotion INTEGER NOT NULL, RegDate TEXT NOT NULL);
